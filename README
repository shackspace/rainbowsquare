== Getting Started

1. Copy config/database.yml.example to config/database.yml and configure the database connections

2. Create the databases: rake db:create:all

2. Migrate the tables: rake db:migrate

2. Start the web server:
       <tt>rails server</tt> (run with --help for options)

3. Go to http://localhost:3000/ and you'll see..
