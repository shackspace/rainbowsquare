<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @game.name %>
</p>

<p>
  <b>Description:</b>
  <%= @game.description %>
</p>

<p>
  <%= form_for(@game, :url => join_game_path, :html => { :method => :post }) do |f| %>
    <%= f.submit :value => 'Join' %>
  <% end %>
</p>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
